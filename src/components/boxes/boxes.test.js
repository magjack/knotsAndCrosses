import React from 'react';
import { shallow } from 'enzyme';
import { addBox, addRow, boxes } from './boxes';

describe('Test the box render method', () => {

    it('Render a span representing a box from an box object', () => {
        
    });
    
});

describe('Test the addRow method works as expected', () => {

    it('It will render a div contain a row of span boxes', () => {
        
    });
    
});